name: Despliegue a servidor AlmaLinux

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Mostrar variables de entorno (debug seguro)
        run: |
          echo "🔐 HOST: ${{ secrets.SERVER_HOST }}"
          echo "👤 USUARIO: ${{ secrets.SERVER_USER }}"
          echo "📦 Puerto SSH: 22"
          echo "🔑 Longitud clave privada: ${#SSH_PRIVATE_KEY}"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Debug SSH key
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key
          echo "📌 Fingerprint de clave privada desde GitHub Secret:"
          ssh-keygen -lf /tmp/deploy_key || echo "❌ ERROR: Clave privada malformada o vacía"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Prueba SSH (diagnóstico)
        run: |
          echo "$SSH_PRIVATE_KEY" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key

          echo "🔐 Derivando clave pública:"
          ssh-keygen -y -f /tmp/deploy_key || echo "❌ ERROR: Clave privada inválida"

          echo "🚪 Probar conexión SSH:"
          ssh -o StrictHostKeyChecking=no -o BatchMode=yes -i /tmp/deploy_key ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} 'echo "✅ Conexión SSH autenticada desde runner" && whoami && pwd' || echo "❌ ERROR: Falló autenticación SSH desde el runner"

          shred -u /tmp/deploy_key || rm -f /tmp/deploy_key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Conectar al servidor y desplegar
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "✅ Conectado exitosamente al servidor."
            cd /root/Escritorio/RegistroCivilFront
            echo "📦 Actualizando código..."
            git pull origin main
            echo "🧹 Deteniendo contenedores existentes..."
            docker compose down
            echo "🚀 Reconstruyendo e iniciando contenedores..."
            docker compose up -d --build
