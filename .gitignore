# 👉 Node.js dependencies
node_modules/

# 👉 Builds y distribución
dist/
build/

# 👉 Archivos de estilos (si no son generados automáticamente, bórralos de aquí)
src/css/
src/css/styles.css

# 👉 Archivos de entorno (env)
.env
.env.local
.env.*.local
.env.development
.env.test
.env.production
.env.development.local
.env.test.local
.env.production.local

# 👉 Archivos de log temporales
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 👉 Sistema operativo
.DS_Store
Thumbs.db

# 👉 Archivos de bloqueo de dependencias (si usas solo uno, ignora los otros)
package-lock.json
yarn.lock
pnpm-lock.yaml

# 👉 IDEs y editores (opcional)
.vscode/
.idea/
