server {
    listen 80;
    server_name prueba.appservices.com.ec;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name prueba.appservices.com.ec;

    ssl_certificate /etc/letsencrypt/live/prueba.appservices.com.ec/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/prueba.appservices.com.ec/privkey.pem;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
